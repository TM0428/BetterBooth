{
  "name": "betterbooth",
  "version": "0.3.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r src/js dist/js && cp -r src/css dist/css && cp -r _locales dist/_locales",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.2.47",
    "vue-router": "^4.2.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.1",
    "vite": "^4.3.4"
  }
}
